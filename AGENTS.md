# Life Workspace Agent Bootstrap

Before substantial work in this repository, read `.agents/AGENTS.md` and follow the Life Workspace protocol.

This root file exists for agent shells that only auto-discover `AGENTS.md` at the workspace root.
